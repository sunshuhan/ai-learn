export const BASICS_CONTENT: Record<string, string> = {
  "ai-for-kids": `
# 🤖 给 5 岁小朋友的 AI 小课堂

## 1. 什么是 AI（人工智能）？
想象一下，你有一个超级聪明的**机器小助手**！🤖
普通机器只会听主人的命令，比如你按一下开关，灯就亮了。
但是 **AI（人工智能）** 不一样，它会**学习**！就像你学习认字、认颜色一样，AI 也会通过看很多很多图片、听很多很多话来变得越来越聪明。

**比喻：**
AI 就像一个**大脑**，住在电脑或者机器人里。它能帮你画画、讲故事，甚至还能帮你认出花园里哪朵花最漂亮！🌸

## 2. 什么是智能体（AI Agent）？
如果说 AI 是一个“大脑”，那**智能体**就是一个**“会动脑筋的小超人”**！🦸‍♂️
它不仅聪明，还会**自己想办法**去完成任务。

比如，你对它说：“我饿了，想吃好吃的。”
- **普通的 AI** 可能会告诉你：“你可以吃苹果。”
- **智能体** 会想：“小朋友饿了，我要看看冰箱里有什么，然后告诉爸爸妈妈帮他洗个苹果，或者准备点小饼干。”

它会**观察**周围的情况，**思考**该怎么做，然后去**执行**！

## 3. 激发你的想象力！🚀
AI 可以帮你把脑子里的神奇想法变成现实：
- 想看一只**穿着宇航服在月球上跳舞的猫咪**吗？AI 能帮你画出来！🐱‍🚀
- 想听一个关于**会飞的巧克力城堡**的故事吗？AI 能讲给你听！🏰🍫

## 4. 精彩视频推荐
这里有一些有趣的小视频，带你走进 AI 的神奇世界：
- [什么是 AI？（动画版）](https://www.youtube.com/watch?v=mJeNghnyt0Y)
- [机器人是如何学习的？](https://www.youtube.com/watch?v=R9OHn5ZF4Uo)
- [未来的 AI 生活是什么样的？](https://www.youtube.com/watch?v=7Pq-S557XQU)


---
**小朋友们，发挥你们的创造力吧！你想让 AI 帮你做什么神奇的事情呢？🌟**
  `,

  "understanding-ai": `
# 什么是人工智能 (AI)？从表征学习到世界模型

要真正理解人工智能，我们必须跳出“机器模仿人”的拟人化视角，深入到其数学与计算的本质。现代 AI 的核心是**在高维空间中进行流形学习（Manifold Learning）与概率映射**。

本章将作为全景地图，为你串联起后续的所有核心技术节点。

\`\`\`mermaid
graph TD
  Data["现实世界数据 (文本/图像/物理状态)"] --> ML["机器学习 (ML)<br/>寻找决策边界"]
  ML --> DL["深度学习 (DL)<br/>高维空间的特征扭曲与折叠"]
  DL --> LLM["大语言模型 (LLM)<br/>压缩人类语言逻辑 (时间序列)"]
  DL --> VLM["视觉语言模型 (VLM)<br/>跨模态对齐 (视觉+语言)"]
  LLM --> Agent["AI Agent<br/>推理与工具调用 (ReAct)"]
  VLM --> Spatial["空间智能与世界模型<br/>理解 3D 物理法则与因果"]
  Spatial --> Embodied["具身智能 (Embodied AI)<br/>在物理世界中行动"]
\`\`\`

## 1. 机器学习 (ML)：寻找高维空间的决策边界
在**机器学习基础**章节中，你会学到 AI 如何从数据中总结规律。
从数学视角看，现实世界的数据（如一张猫的图片）是由成千上万个像素组成的，它存在于一个极高维度的数学空间中。机器学习的本质，就是在这个高维空间中寻找一个“超平面（Hyperplane）”，把“猫”的区域和“狗”的区域切分开来。

## 2. 深度学习 (DL)：空间的扭曲与折叠
当数据极其复杂时（比如猫的姿态千变万化），简单的切割已经失效。在**神经网络与深度学习**章节中，我们将探讨多层神经网络的作用。
深度学习通过一层层的非线性激活函数（如 ReLU），像揉面团一样，将原本纠缠在一起的高维数据空间进行**扭曲、拉伸和折叠**，直到它们变得线性可分。这就是“特征提取”的数学本质。

## 3. 大语言模型 (LLM)：压缩人类认知的隐空间
在**大语言模型**章节，我们将进入 Transformer 的世界。
LLM 并不是在“背诵”文本，而是在进行**数据压缩**。通过预测下一个词（Next-token prediction），LLM 被迫在其内部的隐空间（Latent Space）中构建出对人类语言、逻辑、甚至世界常识的抽象表征。当模型规模突破临界点时，这种深度的压缩催生了**涌现能力（Emergent Abilities）**，让 AI 展现出零样本推理的能力。

## 4. VLM 与多模态：打破模态的巴别塔
语言是人类抽象思维的产物，但世界是多模态的。在 **VLM 与多模态大模型**章节，我们将看到 AI 如何“睁开眼睛”。
通过 CLIP 等对比学习技术，AI 成功地将“图像的像素空间”和“文本的语义空间”强行拉拽到了同一个数学空间中对齐。这使得 AI 不仅能看懂图，还能理解图中的逻辑关系。

## 5. 空间智能与世界模型：从“观察者”到“行动者”
目前的 LLM 和 VLM 仍然是“被动的观察者”，它们缺乏对物理世界因果关系（如重力、遮挡、材质）的深刻理解。
在**空间智能与世界模型**章节，我们将探讨 AI 的下一个圣杯。通过 DiT 架构（如 Sora），AI 开始在隐空间中模拟物理引擎。李飞飞教授提出的“空间智能”，正是要求 AI 不仅能识别 2D 像素，还要理解 3D 空间结构，并最终赋能**具身智能（Embodied AI）**，让机器人能在真实的物理世界中安全地行动。

> **全景总结**：AI 的演进是一场 from “低维到高维”、“从单一模态到多模态”、“从静态观察到动态交互”的升维之旅。理解了这条主线，你就能看懂所有前沿技术的底层逻辑。
  `,

  "ml-basics": `
# 机器学习基础：三大范式与 LLM 的进化

机器学习（Machine Learning）是实现人工智能的核心方法。我们可以通过三种不同的“教学模式”来拆解其三大范式：监督学习、无监督学习、强化学习，并探讨它们与大语言模型（LLM）之间密不可分的关系。

\`\`\`mermaid
graph TD
  A["机器学习 (ML)"] --> B["监督学习 (Supervised)"]
  A --> C["无监督学习 (Unsupervised)"]
  A --> D["强化学习 (Reinforcement)"]
  
  B --> B1["微调 (Fine-tuning)"]
  C --> C1["预训练 (Pre-training)"]
  D --> D1["对齐 (Alignment/RLHF)"]
  
  B1 & C1 & D1 --> LLM["大语言模型 (LLM)"]
\`\`\`

---

### 1. 监督学习 (Supervised Learning)

- **核心思想**：就像有一个老师拿着标准答案在教你。你需要通过学习输入和输出之间的对应关系，来预测新输入的输出。
- **如何使用**：
  1. **准备数据**：收集大量的“输入”和对应的“标签”（正确答案）。例如，训练识别猫的模型，需要标注好图片。
  2. **训练模型**：模型通过不断尝试预测，并对比标准答案来修正自己的错误。
- **在 LLM 中的应用**：**指令微调 (Instruction Tuning)**。我们给模型看成千上万个“问题-理想回答”对，让它学会按照人类的指令来回答问题。

### 2. 无监督学习 (Unsupervised Learning)

- **核心思想**：没有老师，也没有标准答案。你需要自己去发现数据中的规律、结构或模式。
- **如何使用**：
  1. **聚类**：把相似的东西分到一组。
  2. **降维**：简化数据的复杂性。
- **在 LLM 中的应用**：**预训练 (Pre-training)**。这是大模型最关键的一步。模型阅读整个互联网的文本，通过“预测下一个词”来学习语言的规律和世界的知识。这本质上是一种自监督学习（无监督学习的一种）。

### 3. 强化学习 (Reinforcement Learning)

- **核心思想**：就像训练小狗。它做对了就给奖励，做错了就给惩罚。模型通过不断尝试，寻找能获得最大奖励的行动路径。
- **如何使用**：
  1. **智能体**：在环境中行动。
  2. **奖励信号**：环境给出的反馈。
- **在 LLM 中的应用**：**RLHF (基于人类反馈的强化学习)**。让模型生成的回答更符合人类的偏好（有用、诚实、无害）。

> **总结**：大模型的诞生是三大范式的完美结合：无监督学习提供了博大的知识，监督学习教会了它听从指令，强化学习塑造了它的价值观。
  `,

  "neural-networks": `
# 神经网络与深度学习：黑盒内部的齿轮

理清神经网络、深度学习与 Transformer 的关系，能帮助你从底层理解现在所有大模型的工作原理。

\`\`\`mermaid
graph TD
  AI["人工智能 (AI)"] --> ML["机器学习 (Machine Learning)"]
  ML --> DL["深度学习 (Deep Learning)"]
  DL --> CNN["卷积神经网络 (CNN)<br/>擅长图像"]
  DL --> RNN["循环神经网络 (RNN)<br/>擅长序列"]
  DL --> Transformer["Transformer<br/>擅长序列，又快又强"]
  Transformer --> GPT["GPT 系列"]
\`\`\`

## 1. 神经元的数学本质

一个神经元接收来自其他神经元的信号，这些信号有强有弱（权重），当总信号超过某个阈值时，该神经元就会被激活。

数学表达为：\\[ y = f(w_1 x_1 + w_2 x_2 + ... + w_n x_n + b) \\]

- **权重 (w)**：代表信号的重要性。
- **偏置 (b)**：代表激活的难易程度。
- **激活函数 (f)**：引入非线性，让网络能处理复杂问题（如 ReLU）。

## 2. 前馈神经网络

把大量神经元按层排列，信息只从输入层向输出层单向流动。
- **输入层**：接收原始数据。
- **隐藏层**：负责提取特征，层数越多，提取的特征越抽象。
- **输出层**：产生最终结果。

## 3. 反向传播 (Backpropagation)

这是神经网络学习的核心算法。
1. **前向传播**：计算输出和损失（误差）。
2. **反向传播**：利用链式法则，从后往前计算每个权重对误差的贡献（梯度）。
3. **更新权重**：根据梯度调整权重，减小误差。

## 4. 深度学习 (Deep Learning)

深度学习就是使用**有很多隐藏层的神经网络**。这种“深”使得模型能够自动从原始数据中逐层提取越来越抽象的特征（例如：从边缘到五官再到人脸）。
  `,

  "llm": `
# 大语言模型 (LLM)：Transformer 与涌现能力

大语言模型（如 GPT-4, Gemini）是当今 AI 的巅峰之作。它们的核心架构是 2017 年提出的 **Transformer**。

\`\`\`mermaid
graph TD
  A["输入文本"] --> B["Tokenization 词元化"]
  B --> C["Embedding 词向量化 + 位置编码"]
  C --> D["Transformer Block"]
  subgraph TB ["Transformer Block"]
    E["Multi-Head Self-Attention 多头自注意力"]
    F["Feed Forward 前馈神经网络"]
    E --> F
  end
  D --> G["Softmax 概率分布"]
  G --> H["预测下一个 Token"]
\`\`\`

## 1. Embedding (嵌入)：文字的数学身份

计算机不认识字，只认识数字。Embedding 将每个词映射到一个高维空间中的向量。
- **语义相近，距离相近**：在数学空间里，“猫”和“狗”的距离很近，而和“汽车”很远。
- **捕捉关系**：甚至能理解“国王 - 男人 + 女人 = 王后”。

## 2. 自注意力机制 (Self-Attention)

这是 Transformer 的灵魂。它让模型在处理一个词时，能同时关注句子中所有其他词。
- **Q (Query)**：我想找什么？
- **K (Key)**：我有什么？
- **V (Value)**：我具体的内容是什么？
通过 Q 和 K 的匹配，模型决定应该把多少“注意力”放在哪个词上。

## 3. 训练三部曲

1. **预训练 (Pre-training)**：阅读海量文本，通过“预测下一个词”获得博大的知识。
2. **监督微调 (SFT)**：通过高质量问答对，教模型学会对话和遵循指令。
3. **强化学习 (RLHF)**：通过人类反馈，让模型更符合人类的偏好和价值观。

## 4. 涌现能力 (Emergent Abilities)

当模型规模突破某个临界点时，模型会突然展现出之前完全不具备的能力，如逻辑推理、多步规划等。
  `,

  "vlm": `
# VLM 与多模态大模型：让 AI 睁开眼睛

纯文本的 LLM 只能理解符号，而 **VLM (Vision-Language Model)** 赋予了 AI 理解物理世界视觉信息的能力。

## 1. 视觉 Transformer (ViT)
图像被切成小方块（Patches），当成“单词”送进 Transformer 架构中处理。

## 2. 对比学习 (CLIP)
通过海量图文对训练，让 AI 成功地将“图像空间”和“文本空间”对齐。

## 3. VLM 的应用
- **图像描述**：自动生成图片的文字说明。
- **视觉问答**：根据图片回答复杂问题。
- **视觉推理**：理解图中的逻辑关系。
  `,

  "spatial-models": `
# 空间智能与世界模型：理解物理法则

**世界模型 (World Models)** 的目标是让 AI 掌握现实世界的物理法则和三维空间结构。

## 1. 什么是世界模型？
人类能在脑海中模拟“如果我推杯子，它会掉下去”。世界模型让 AI 在隐空间中建立一个包含物理法则的虚拟引擎。

## 2. Sora 与 DiT 架构
Sora 通过时空补丁（Spacetime Patches）和 Transformer 架构，实现了长视频生成中的物理一致性。

## 3. 空间智能
AI 不仅要识别物体，还要理解它们在三维空间中的关系，这是通往**具身智能 (Embodied AI)** 的关键。
  `,

  "ai-agents": `
# AI Agent 智能体：从“大脑”到“数字员工”

**AI Agent (智能体)** 是给大模型装上了记忆、手脚和感官，让它能够自主完成任务。

## 1. 四大核心组件
1. **角色设定 (Profile)**：定义身份和目标。
2. **记忆系统 (Memory)**：短期记忆（上下文）与长期记忆（向量数据库）。
3. **规划 (Planning)**：任务拆解与自我反思。
4. **行动 (Action)**：调用外部工具（搜索、代码执行、API）。

## 2. ReAct 运行机制
通过 **Reasoning (推理)** 和 **Acting (行动)** 的不断循环，Agent 能够一步步解决复杂问题。
  `,

  "vector-db": `
# 向量数据库：AI 的“长期记忆”

向量数据库专门用来存储和查询向量数据，是实现 **RAG (检索增强生成)** 的核心。

## 1. 核心原理
- **向量化**：将非结构化数据转换为高维向量。
- **相似性度量**：通过计算向量间的距离（如余弦相似度）来查找相关内容。
- **向量索引**：如 HNSW 算法，大幅提升大规模数据的检索速度。

## 2. 应用场景
- **语义搜索**：理解搜索意图而非仅仅关键词匹配。
- **知识库问答**：让 AI 基于私有文档生成准确回答。
  `,

  "llm-creativity": `
# 成语接龙 vs 创造性：如何让大模型具有创造性？

大模型本质上是“预测下一个词”，但通过以下方式可以激发其创造性：

1. **调整生成策略**：提高温度（Temperature）参数，引入随机性。
2. **融合外部知识**：通过 RAG 结合最新信息。
3. **强化学习 (RLHF)**：引导模型追求“有趣”和“新颖”。
4. **多模型协作**：通过辩论和反思产生更成熟的创意。
  `,

  "scaling-laws": `
# 规模法则 (Scaling Laws)：为什么 AI 越来越大？

**Scaling Laws** 揭示了模型性能与计算量、数据量、以及参数规模之间的幂律关系。

## 1. 核心要素
- **计算量 (Compute)**
- **数据集大小 (Data Size)**
- **参数量 (Parameters)**

## 2. 涌现现象
当规模跨过临界点，模型会突然展现出逻辑推理等高级能力。

## 3. 未来趋势
- **数据枯竭**：高质量数据快被用光了。
- **合成数据**：用 AI 生成数据来训练 AI。
  `,

  "peft-lora": `
# PEFT 与 LoRA：如何高效地“调教”大模型？

**PEFT (Parameter-Efficient Fine-Tuning)** 让我们只需更新极少数参数就能微调大模型。

## 1. LoRA (低秩适配)
- **原理**：引入低秩矩阵，冻结原始权重，只训练新增的小矩阵。
- **优势**：显存占用极低，权重可插拔。

## 2. 为什么需要微调？
- **领域适配**（医疗、法律）。
- **格式控制**（严格输出 JSON）。
- **角色扮演**。
  `,

  "slm-on-device": `
# SLM 与端侧 AI：AI 正在“瘦身”进入你的手机

**SLM (Small Language Models)** 正在让 AI 从云端走向本地设备。

## 1. 为什么需要小模型？
- **隐私安全**：数据不出本地。
- **低延迟**：响应近乎实时。
- **低成本**：无需昂贵的 API 费用。

## 2. 如何让小模型变强？
- **高质量数据**：用“教科书”级别的数据训练。
- **知识蒸馏**：让大模型教小模型。
  `
};
